---
layout: default
---
<article class="post content">
  <header>
    <time class="text-sm uppercase" datetime="{{ page.date | date: "%Y-%d-%m" }}">
      {{ page.date | date: "%b %d, %Y" }}
    </time>
    <h1 class="">{{ page.title }}</h1>
  </header>
  {{ content }}
</article>
